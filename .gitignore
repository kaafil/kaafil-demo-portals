node_modules/
.next/
out/
.env
*.tsbuildinfo
next-env.d.ts

# Rebuilt from fixtures on every `pnpm seed`. Never commit it — a stale
# checked-in database is indistinguishable from a seeding bug.
crm.sqlite
crm.sqlite-wal
crm.sqlite-shm

# Generated by `pnpm seed:bulk`. Deterministic, so regenerating it is free.
fixtures/bulk.generated.json

.DS_Store

# Bundled from sw/ by `pnpm build:sw`. Generated, never edited by hand.
public/sw.js

# A defect list written against Kaafil while integrating. Kept as a local
# record; this repo is public and the file reads as a bug report, not a demo.
docs/FINDINGS.md
